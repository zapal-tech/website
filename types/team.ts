export type TeamMemberPreview = {
  firstName: string;
  lastName: string;
  title: string;
  imageUrl: string;
};

export type TeamMember = {
  firstName: string;
  lastName: string;
  title: string;
  email: string;
  imageUrl: string;
  description: string;
  linkedInUrl: string;
};
